.env*
dist

# Created by https://www.toptal.com/developers/gitignore/api/go
# Edit at https://www.toptal.com/developers/gitignore?templates=go

### Go ###
# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# End of https://www.toptal.com/developers/gitignore/api/go

# rapid が失敗ケースを記録する一時ファイル
**/testdata/rapid/

# 計算結果。入力から常に作り直せる（決定 4）
out/

# make が組み立てる lifeplan バイナリ。ソースから常に作り直せる
bin/

# 差分から組み立てる scenario の表。もとの表と .diff.tsv から
# Makefile が作り直す
data/controllable/scenario/income-wife-parttime.tsv
data/controllable/scenario/income-wife-business.tsv
